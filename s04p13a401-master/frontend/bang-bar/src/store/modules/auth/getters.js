export default {
    userName(state) {
      return state.user_name;
    }, 
    pkUser(state) {
      return state.pk_user;
    },
    userEmail(state) {
      return state.email;
    },
    profileImage(state) {
      return state.profileImage;
    }
  };